#include <stdio.h>
/**
 * main - A program  prints a line  with printf function
 * Return: 0 (Success when the 0 is returned)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}

